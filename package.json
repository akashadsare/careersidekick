{
  "name": "careersidekick",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "check": "npm --prefix frontend run check",
    "test": "npm --prefix frontend run test",
    "backend:test": "bash -lc 'cd backend && source .venv/bin/activate && pytest -q'",
    "backend:lint": "bash -lc 'cd backend && source .venv/bin/activate && ruff check .'"
  }
}
